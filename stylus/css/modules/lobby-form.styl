.lobby-form {
    // special fade-out animation
    &.out {
        -webkit-transform: translateY(200px);
        transform: translateY(200px);
        transition: .25s; // transition all properties, including our transform
    }
}